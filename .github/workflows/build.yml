name: Docker Image Build&Push
on:
  push:
    branches:
      - "master"
      - "main"
  pull_request:
    branches:
      - "master"
      - "main"
  workflow_dispatch:

env:
  IMAGE_NAME: flaskapp
  IMAGE_VER: v0.1
  DOCKERHUB_USERNAME: soutrik71
  DOCKERHUB_TOKEN: dckr_pat_gYWOrmlTANxhhV3FSu-yDqp9xEI

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - name: Code checkin
        uses: actions/checkout@v2

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v1

      - name: Setup docker buildx
        uses: docker/setup-buildx-action@v2

      - name: login into Docker hub
        uses: docker/login-actions@v2
        with:
          username: ${{ env.DOCKER_HUB_USERNAME }}
          password: ${{ env.DOCKER_HUB_ACCESS_TOKEN }}

      - name: Build and Push
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ env.DOCKER_HUB_USERNAME }}/${{ env.IMAGE_NAME }}:${{ env.IMAGE_VER }}
